[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[project]
name = "marinskyrl"
version = "0.1.0"
description = "Marin's typed launcher and RL training runtime"
requires-python = "==3.12.*"
dependencies = [
    "boto3",
    "datasets>=4.0.0",
    "debugpy==1.8.0",
    "fastapi",
    "filelock>=3.16",
    "fsspec>=2024.0.0",
    "func_timeout",
    "hf_transfer",
    "huggingface-hub>=0.34.0",
    "math-verify==0.8.0",
    "hydra-core==1.3.2",
    "jaxtyping",
    "loguru",
    "marin-finelog",
    "marin-finelog-server",
    "marin-iris",
    "marin-iris-native",
    "ninja",
    "nvidia-ml-py",
    "omegaconf",
    "openapi-schema-validator",
    "pandas",
    "polars",
    "pyarrow>=18",
    "pyyaml>=6.0",
    "ray==2.51.1",
    "requests",
    "reasoning-gym==0.1.25",
    "s3fs",
    "smart_open",
    "tensorboard",
    "tqdm>=4.66",
    "tokenizers==0.23.1",
    "transformers==5.16.1",
    "verifiable-instructions @ git+https://github.com/abukharin-nv/verifiable-instructions.git@f46a5ac87b1400a4f8973039844b6be9b56e3faf",
    "xmltodict",
    "uvicorn",
    "wandb",
]

[project.optional-dependencies]
cpu = [
    "accelerate>=1.14,<2",
    "harbor-config",
    "peft>=0.19",
    "tensordict",
    "torch==2.11.0",
    "torchdata",
]
cuda = [
    "cuda-toolkit[nvcc,cccl]==13.2.1 ; sys_platform == 'linux'",
    "accelerate>=1.14,<2 ; sys_platform == 'linux'",
    "harbor-config ; sys_platform == 'linux'",
    "peft>=0.19 ; sys_platform == 'linux'",
    "tensordict ; sys_platform == 'linux'",
    "torch==2.13.0 ; sys_platform == 'linux'",
    "torchdata ; sys_platform == 'linux'",
    "torchvision==0.28.0 ; sys_platform == 'linux'",
]
deepspeed = [
    "cuda-toolkit[nvcc,cccl]==13.2.1 ; sys_platform == 'linux'",
    "accelerate>=1.14,<2 ; sys_platform == 'linux'",
    "deepspeed==0.17.6 ; sys_platform == 'linux'",
    "harbor-config ; sys_platform == 'linux'",
    "peft>=0.19 ; sys_platform == 'linux'",
    "tensordict ; sys_platform == 'linux'",
    "torch==2.13.0 ; sys_platform == 'linux'",
    "torchdata ; sys_platform == 'linux'",
    "torchvision==0.28.0 ; sys_platform == 'linux'",
]
fsdp = [
    "flash-attn==2.8.3 ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "torch==2.13.0 ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "torchtitan ; sys_platform == 'linux' and platform_machine == 'x86_64'",
]
vllm = [
    # The CPU audio wheel uses Torch for GPU resampling without loading a cu132 extension.
    "torchaudio==2.11.0+cpu ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "torchcodec==0.16.0+cu132 ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "cuda-toolkit[nvcc,cccl]==13.2.1 ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "accelerate>=1.14,<2 ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "flashinfer-cubin==0.6.18.post1 ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "flashinfer-python==0.6.18.post1 ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "harbor[analysis,datasets,daytona] ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "harbor-config ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "peft>=0.19 ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "quack-kernels==0.6.4 ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "tensordict ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "torch==2.13.0 ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "torchdata ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "torchvision==0.28.0 ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "vllm==0.0.0.dev20260912+marin.177d960f5b43.cu132 ; sys_platform == 'linux' and platform_machine == 'x86_64'",
]
megatron = [
    "cuda-toolkit[nvcc,cccl]==13.2.1 ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "accelerate>=1.14,<2 ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "causal-conv1d>=1.5,<2 ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "flash-linear-attention==0.4.2 ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "mamba-ssm>=2.2,<3 ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "megatron-bridge==0.6.0 ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "megatron-core==0.18.0 ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "harbor-config ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "nvidia-modelopt==0.46.1 ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "peft>=0.19 ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "tensordict ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "torch==2.13.0 ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "torchdata ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "torchvision==0.28.0 ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "transformer-engine[pytorch]==2.11.0 ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "transformer-engine-cu13==2.11.0 ; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "transformer-engine-torch==2.11.0 ; sys_platform == 'linux' and platform_machine == 'x86_64'",
]
telemetry = [
    "marin-rigging[prometheus]",
    "memray ; sys_platform == 'linux'",
    "ray[default]",
]

[project.scripts]
marinskyrl = "cloud.iris.job:main"

[dependency-groups]
analysis = [
    "matplotlib",
    "pandas",
    "tabulate",
]
docs = [
    "myst-parser>=2.0.0",
    "sphinx>=7.0.0",
    "sphinx-autobuild>=2021.3.14",
    "sphinx-autodoc-typehints>=1.25.0",
    "sphinx-copybutton>=0.5.0",
    "sphinx-rtd-theme>=2.0.0",
]
dev = [
    { include-group = "analysis" },
    "litellm",
    "marin-rigging[prometheus]",
    "marin-style @ git+https://github.com/marin-community/marin-style@5094279da60b47b9a8fa8effaf7f73cd13f1e96f",
    "prometheus-client",
    "pytest>=8.4",
    "pytest-asyncio",
    "universal-pathlib",
]
harbor-test = ["harbor; sys_platform == 'linux'"]

[tool.hatch.build.targets.wheel.force-include]
"chat_templates" = "chat_templates"
"cloud/iris" = "cloud/iris"
"skyrl-gym/skyrl_gym" = "skyrl_gym"
"skyrl-train/skyrl_train" = "skyrl_train"

[tool.uv]
package = true
prerelease = "allow"
required-version = ">=0.8.10"
conflicts = [
    [{ extra = "cpu" }, { extra = "cuda" }],
    [{ extra = "cpu" }, { extra = "deepspeed" }],
    [{ extra = "cpu" }, { extra = "fsdp" }],
    [{ extra = "cpu" }, { extra = "megatron" }],
    [{ extra = "cpu" }, { extra = "vllm" }],
]
environments = [
    "sys_platform == 'darwin'",
    "sys_platform == 'linux' and platform_machine == 'x86_64'",
    "sys_platform == 'linux' and platform_machine == 'aarch64'",
]
no-build-isolation-package = [
    "transformer-engine",
    "transformer-engine-torch",
]
override-dependencies = [
    # Megatron Bridge declares this native extension but does not import or use it.
    "fast-hadamard-transform ; sys_platform == 'never'",
    "nvidia-resiliency-ext ; sys_platform == 'never'",
    "fsspec==2026.4.0",
    # Megatron Bridge pins older serving libraries; share the selected vLLM ABI.
    "flashinfer-python==0.6.18.post1 ; sys_platform == 'linux'",
    "flashinfer-cubin==0.6.18.post1 ; sys_platform == 'linux'",
    "transformers==5.16.1",
    "opentelemetry-api==1.44.0 ; sys_platform == 'linux'",
    "opentelemetry-api ; sys_platform != 'linux'",
]
constraint-dependencies = [
    "botocore==1.42.97 ; sys_platform == 'linux'",
    "boto3==1.42.97 ; sys_platform == 'linux'",
    "s3transfer==0.16.1 ; sys_platform == 'linux'",
    "aiobotocore==3.7.0 ; sys_platform == 'linux'",
    "s3fs==2026.4.0 ; sys_platform == 'linux'",
]

[tool.uv.extra-build-dependencies]
transformer-engine = [{ requirement = "torch", match-runtime = true }, "build_tools", "ninja"]
transformer-engine-torch = [{ requirement = "torch", match-runtime = true }, "build_tools", "ninja"]

[tool.uv.sources]
torchaudio = { index = "pytorch-cpu", marker = "sys_platform == 'linux' and platform_machine == 'x86_64'" }
torchcodec = { index = "pytorch-cu132", extra = "vllm", marker = "sys_platform == 'linux' and platform_machine == 'x86_64'" }
flash-attn = [
    { url = "https://github.com/marin-community/MarinSkyRL/releases/download/native-cu132-torch2.13-20260910-6d12d7a0/flash_attn-2.8.3-cp312-cp312-linux_x86_64.whl", marker = "sys_platform == 'linux' and platform_machine == 'x86_64'" },
]
harbor = { git = "https://github.com/marin-community/harbor.git" }
torch = [
    { index = "pytorch-cpu", extra = "cpu", marker = "sys_platform == 'linux' and platform_machine == 'x86_64'" },
    { index = "pytorch-cu132", extra = "cuda" },
    { index = "pytorch-cu132", extra = "deepspeed" },
    { index = "pytorch-cu132", extra = "fsdp" },
    { index = "pytorch-cu132", extra = "megatron" },
    { index = "pytorch-cu132", extra = "vllm" },
]
torchvision = [
    { index = "pytorch-cu132", extra = "cuda" },
    { index = "pytorch-cu132", extra = "deepspeed" },
    { index = "pytorch-cu132", extra = "megatron" },
    { index = "pytorch-cu132", extra = "vllm" },
]
flashinfer-cubin = { index = "flashinfer", marker = "sys_platform == 'linux'" }
vllm = [
    { url = "https://github.com/marin-community/vllm/releases/download/marin-vllm-gpu-20260912-177d960f5b43/vllm-0.0.0.dev20260912+marin.177d960f5b43.cu132-cp38-abi3-manylinux_2_28_x86_64.whl", marker = "sys_platform == 'linux' and platform_machine == 'x86_64'" },
]
torchtitan = { git = "https://github.com/pytorch/torchtitan", rev = "a1fdd7e" }
transformer-engine-torch = [
    { url = "https://github.com/marin-community/MarinSkyRL/releases/download/native-cu132-torch2.13-20260910-6d12d7a0/transformer_engine_torch-2.11.0-cp312-cp312-linux_x86_64.whl", marker = "sys_platform == 'linux' and platform_machine == 'x86_64'" },
]
causal-conv1d = [
    { url = "https://github.com/marin-community/MarinSkyRL/releases/download/native-cu132-torch2.13-20260910-6d12d7a0/causal_conv1d-1.6.1-cp312-cp312-linux_x86_64.whl", marker = "sys_platform == 'linux' and platform_machine == 'x86_64'" },
]
mamba-ssm = [
    { url = "https://github.com/marin-community/MarinSkyRL/releases/download/native-cu132-torch2.13-20260910-6d12d7a0/mamba_ssm-2.3.1-cp312-cp312-linux_x86_64.whl", marker = "sys_platform == 'linux' and platform_machine == 'x86_64'" },
]
harbor-config = { url = "https://github.com/marin-community/harbor/releases/download/harbor-config-952a005353273590631b8f970f4664a121710b32/harbor_config-0.1.0-py3-none-any.whl" }

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[[tool.uv.index]]
name = "pytorch-cu132"
url = "https://download.pytorch.org/whl/cu132"
explicit = true

[[tool.uv.index]]
name = "flashinfer"
url = "https://flashinfer.ai/whl"
explicit = true

[tool.marin-style]
checks = ["ruff-check", "ruff-format"]
ruff_version = "0.14.3"
include = ["marinskyrl/**", "skyrl-train/**", "skyrl-gym/**", "skyrl-tx/**", "cloud/**", "scripts/**"]

[tool.ruff]
line-length = 120

[tool.ruff.lint]
ignore = [
    "F722",
    "E741",
]

[tool.ruff.lint.per-file-ignores]
"skyrl-train/tests/gpu/test_e2e_moe_rl_step.py" = ["E402"]
"skyrl-train/tests/gpu/test_e2e_moe_rl_step_80b.py" = ["E402"]
"skyrl-train/tests/cpu/distributed/test_muon_hybrid.py" = ["E402"]
"skyrl-train/tests/cpu/models/test_moe_weight_remap.py" = ["E402"]
"skyrl-train/tests/cpu/models/test_models.py" = ["E402"]
"skyrl-train/tests/cpu/test_ray_metrics.py" = ["E402"]
"skyrl-train/skyrl_train/trajectory_runners/harbor/_harbor_compat.py" = ["F401"]

[tool.pytest.ini_options]
addopts = "-v -s"
testpaths = ["skyrl-train/tests", "cloud/iris/tests"]
markers = [
    "vllm",
    "integrations",
    "megatron",
]
