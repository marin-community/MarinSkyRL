name: Ops - Agent Prose Cleanup (disabled)

on:
  issues:
    types: [opened, edited, reopened, labeled]
  pull_request_target:
    types: [opened, edited, reopened, labeled]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.issue.number || github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  request-cleanup:
    if: contains(github.event.issue.labels.*.name || github.event.pull_request.labels.*.name, 'agent-generated')
    runs-on: ubuntu-latest
    steps:
      - uses: marin-community/marin-style/actions/prose-cleanup@main
        with:
          trigger-token: ${{ secrets.LOOM_TRIGGER_GH_TOKEN }}
