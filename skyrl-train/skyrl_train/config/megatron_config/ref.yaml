# @package megatron_config.ref
tensor_model_parallel_size: 1
pipeline_model_parallel_size: 1
context_parallel_size: 1
expert_model_parallel_size: 1
expert_tensor_parallel_size: 1


model_config_kwargs: {}

# kwargs to override the Megatron TransformerConfig object
transformer_config_kwargs: {}
# Sequence-dim chunk size for the vocab-parallel logprob computation.
# null (default) materializes the full [B, S, vocab//TP] fp32 exp at once (can
# OOM on long sequences); an int activates the numerically-exact chunked path
# (ChunkedDistributedLogprob), bounding peak memory regardless of seq length.
logprob_chunk_size: null
