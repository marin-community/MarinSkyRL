Use three separate shell tool calls. First run `printf 'first-06\n' > /app/proof.txt`. After it returns, run `printf 'second-06\n' >> /app/proof.txt`. After that returns, run `cat /app/proof.txt`. Do not combine the commands; finish after seeing both lines.

